import { Canvas, Meta } from '@storybook/blocks';

import * as BackToTopStories from './back-to-top.stories.svelte';

<Meta of={BackToTopStories} />

# Back to top

The Back to top component is used to display a button that allows the user to scroll back to the top of the page.
It appears hidden until the user has scrolled a sufficient amount.

## Usage

### Basic

The component should appear at the bottom of the structure of your page.

<Canvas of={BackToTopStories.Basic} sourceState="shown" />