import { Canvas, Meta } from '@storybook/blocks';

import * as AlertStories from './alert.stories.svelte';

<Meta of={AlertStories} />

# Alert

The alert component is used to display information that should be catching the user's attention.
It is typically paired with our modal component.

## Usage

### Basic

<Canvas of={AlertStories.Basic} sourceState="shown" />

### With a subtitle

<Canvas of={AlertStories.WithSubtitle} sourceState="shown" />

### With a footer

<Canvas of={AlertStories.WithFooter} sourceState="shown" />

### With a message that contains a link
<Canvas of={AlertStories.WithMessageContainingLink} sourceState="shown" />

### Color Variants

<Canvas of={AlertStories.Yellow} sourceState="shown" />
<Canvas of={AlertStories.Blue} sourceState="shown" />
<Canvas of={AlertStories.Green} sourceState="shown" />

## Properties

<table>
  <thead>
    <tr>
      <th>Property</th>
      <th>Attribute</th>
      <th>Description</th>
      <th>Type</th>
      <th>Default</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>
        <code>color</code>
      </td>
      <td>
        <code>color</code>
      </td>
      <td>
        Used to determine the color of the top bar in the alert.
      </td>
      <td>
        <code>"red" | "yellow" | "green" | "blue"</code>
      </td>
      <td>
        <code>red</code>
      </td>
    </tr>
  </tbody>
</table>